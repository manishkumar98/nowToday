/*import { configureStore } from "redux-toolkit";
import { childIds, increment, node } from "/reducers/index";
import Tree from "./Tree";
export default configureStore(
  (reducer: {
    childIdsReducer: childIds,
    incrementReducer: increment,
    nodeReducer: node
  })
);
*/
